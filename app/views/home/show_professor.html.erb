<div class="container">
  <p id="notice"><%= notice %></p>
  <h2 class="title">Lista de profesores</h2>
    <div class="search-bar">
      <%= form_tag(show_professor_path, method: :get) do %>
        <%= text_field_tag :search %>
        <%= select_tag :professor_type,"<option>Todos</option><option>Catedra</option><option>Planta</option>".html_safe %>
        <%= submit_tag "Buscar",:class=>"btn btn-outline-secondary" %>
      <% end %>
    </div>
  <div class="cards">
    <% @professors.each do |professor| %>
        <div class="card">
         <%= link_to home_professors_path(professor.id) do %>
            <div class="card__inner">
              <i class="fa fa-user-tie"></i>
              <p><%= professor.name %> <%= professor.father_last_name %> <%= professor.mother_last_name %></p>
            </div>
          <%end%>
        </div>
    <%end%>
  </div>
</div>
