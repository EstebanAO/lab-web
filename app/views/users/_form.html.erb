<div class="container">
  <%= form_with(html:{ class:"form" }, model: user, local: true) do |form| %>
    <% if user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= form.label :Correo %>
      <%= form.text_field :email, class:"form-control" , placeholder: "Correo" %>
    </div>

    <div class="field">
      <%= form.label :Contraseña %>
      <%= form.text_field :password, class:"form-control" , placeholder: "Contraseña" %>
    </div>

    <div class="field">
      <%= form.label :Confirmar_Contraseña %>
      <%= form.text_field :password_confirmation, class:"form-control" , placeholder: "Confirmar Contraseña" %>
    </div>

    <% if @curr_user.admin %>
      <div class="field">
        <%= form.label :admin %>
        <%= form.check_box :admin %>
      </div>
    <% end %>

    <div class="actions" style="padding-top:10px;">
      <%= form.submit 'Guardar', :class => 'btn btn-lg btn-primary btn-block' %>
    </div>
<% end %>
</div>
